spring:
  application:
    name: account-service
  cloud:
    config:
      enabled: 'true'
      uri: http://localhost:8081      
#feign.circuitbreaker.enabled=true
feign:
  circuitbreaker:
    enabled: true